# horizon-policy

Responsibilities:

- validate signed policy bundles against `schemas/policy/`
- materialize effective policy deterministically
- publish policy state over `org.horizon.Policy1`
- emit auditable policy change events
