# HorizonOS Shell

Shell code is the untrusted web UI rendered by the trusted shell host.

The shell must only access privileged capabilities through the versioned `HorizonSystemBridge` contract in `docs/contracts/horizon-system-bridge-v1.md`.
